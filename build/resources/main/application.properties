spring.application.name=Sekwang

spring.jpa.properties.hibernate.show_sql=true
spring.jpa.hibernate.ddl-auto=update

spring.profiles.include = db

# --- JPA / Hibernate ---
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- Jackson ---
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false

# --- Server ---
server.port=8080

# --- JWT ---
jwt.secret=changasdfasdfe-asdfasdfthis-to-a-verasdfasdfasdfecret-sasdfasdftring-at-leasasdfasdft-64-bytes
jwt.expiration-minutes=120

# --- CORS (comma-separated) ---
cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://your-frontend-domain
cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true